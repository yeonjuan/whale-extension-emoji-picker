{
  "name": "whale-extension-emoji-picker",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "parcel": "^2.12.0",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "prettier": "^3.3.3"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm -rf dist && parcel build src/index.html"
  },
  "staticFiles": {
    "staticPath": "static"
  }
}
